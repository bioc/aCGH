require(cluster) || stop("requires cluster package")
require(survival) || stop("requires survival package")
require(multtest) || stop("requires multtest package")
require(sma) || stop("requires sma package")
